<%= form_for @valorar do |f| %>
	
<%= render 'shared/error_messages', object: f.object %>

<div class="form-group">
	<%= f.label :Puntaje, class: 'control-label' %>
	
	<%= f.select(:promedio, [1,2,3,4,5], {}, { :class => 'my_style_class' }) %>
</div>

<div class="form-group">

	<%= f.label :comentario, class: 'control-label' %>

	<%= f.text_field :comentario, placeholder: 'Deje su opinion aqui', class: 'form-control' %>

</div>

	<%= f.submit 'Valorar', class: 'btn btn-primary btn-lg'%>

	
<% end %>
